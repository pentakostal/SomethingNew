<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
        }

        .topnav {
            overflow: hidden;
            background-color: #333;
        }

        .topnav a {
            float: left;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 17px;
        }

        .topnav a:hover {
            background-color: #ddd;
            color: black;
        }

        .topnav a.active {
            background-color: #04AA6D;
            color: white;
        }

        table, th, td {
            border:1px solid black;
        }
    </style>
</head>
<body>

<div class="topnav">
    <a class="active" href="/user">Home</a>
    <a href="/logOut">Log Out</a>
    <a href="/personalCabinet">Personal cabinet</a>
    <a href="/shortStocks">Short operations</a>
</div>

<div style="padding-left:16px">
    <p>{{ status }}</p>
    <h2>Personal cabinet</h2>
    <form class="center" method="post" action="/addMoney">
        <br><label>Add money to wallet</label>
        <br>
        <input type="text" id="addMoney" name="addMoney">
        <input type="submit" value="Add">
    </form>
    <br>
    <form class="center" method="post" action="/sell">
        <br><label>Stock to Sell</label>
        <br>
        <input type="text" id="sellStock" name="sellStock">
        <br><label>Amount of stock to sell</label>
        <br>
        <input type="text" id="sellStockAmount" name="sellStockAmount">
        <input type="submit" value="SELL">
    </form>
    <br>
    <p>Your wallet is: {{ amount }}</p>
    <br>
    <h2>Transaction history</h2>
    <table style="width:90%">
        <tr>
            <th>Transaction ID</th>
            <th>Amount</th>
            <th>Price</th>
            <th>Symbol</th>
            <th>Status</th>
            <th>Date</th>
        </tr>
        <tr>
            {% for record in records %}
        <tr>
            <td><p>{{ record.getBuyId }}</p></td>
            <td><p>{{ record.getAmount }}</p></td>
            <td><p>{{ record.getPrice }}</p></td>
            <td><p>{{ record.getSymbol }}</p></td>
            <td><p>{{ record.getStatus }}</p></td>
            <td><p>{{ record.getDate }}</p></td>
        </tr>
        {% endfor %}
        </tr>
    </table>

    <h2>Stock Bank</h2>
    <table style="width:50%">
        <tr>
            <th>Symbol</th>
            <th>Amount</th>
        </tr>
        <tr>
            {% for stock in stockBank %}
        <tr>
            <td><p>{{ stock.getSymbol }}</p></td>
            <td><p>{{ stock.getAmount }}</p></td>
        </tr>
        {% endfor %}
        </tr>
    </table>
    <br>
    <h2>Transfer stock</h2>
    <form class="center" method="post" action="/transfer">
        <br><label>Stock to transfer (Symbol)</label>
        <br>
        <input type="text" id="transferStock" name="transferStock">
        <br><label>Amount of stock to transfer</label>
        <br>
        <input type="text" id="transferStockAmount" name="transferStockAmount">
        <br><label>User to transfer</label>
        <br>
        <input type="text" id="transferUser" name="transferUser">
        <br><label>Password</label>
        <br>
        <input type="password" name="password" id="password" required>
        <br>
        <br>
        <br><input type="submit" value="TRANSFER">
        <br>
        <br>
        <br>
    </form>
</div>

</body>
</html>